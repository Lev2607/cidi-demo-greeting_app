name: Checkout

on: [push]

jobs:
  strategy:
    matrix:
      os: [ubuntu-latest, windows-latest, macos-latest]
  build:
    runs-on: ${{ matrix.os }}
    steps:
    - name: checkout
      uses: actions/checkout@v4